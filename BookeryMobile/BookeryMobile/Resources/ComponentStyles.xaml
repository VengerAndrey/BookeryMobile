<?xml version="1.0" encoding="utf-8"?>

<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit">
    <Style x:Key="DefaultEntry" TargetType="Entry">
        <Setter Property="TextColor" Value="{DynamicResource Foreground}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource Foreground}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource Primary}" />
    </Style>
    
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="TextColor" Value="{DynamicResource Foreground}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource Primary}" />
        <Setter Property="FontSize" Value="20" />
    </Style>
    
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="TextColor" Value="{DynamicResource Foreground}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource Primary}" />
        <Setter Property="FontSize" Value="16" />
    </Style>
    
    <Style x:Key="SwipeIcon" TargetType="Image">
        <Setter Property="Aspect" Value="AspectFit" />
        <Setter Property="Margin" Value="5" />
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="xct:IconTintColorEffect.TintColor" Value="{DynamicResource SecondaryForeground}" />
    </Style>
</ResourceDictionary>